import { nodata } from "../assets";

const Documents = () => {
  return (
    <div className="all">
      <div>No Data</div>
      <img src={nodata} alt="" />
    </div>
  );
};

export default Documents;
