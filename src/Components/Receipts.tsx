import { nodata } from "../assets";

const Receipts = () => {
  return (
    <div className="receipts">
      <div>No Data</div>
      <img src={nodata} alt="" />
    </div>
  );
};

export default Receipts;
